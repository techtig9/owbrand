/**
 * Test-only stand-in for the `server-only` package.
 *
 * See the alias comment in vitest.config.ts. Importing the real package under
 * Vitest resolves its browser entry, which throws by design.
 */
export {};
